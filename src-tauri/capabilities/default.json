{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Sonora",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default"
  ]
}
